<!-- Fixed navbar -->
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'Location de bateau', root_path, {:class=>"navbar-brand"} %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <% if(!model_signed_in?) %>
           <li><%= link_to 'Inscription', new_model_registration_path %></li> 
          <li><%= link_to 'Connexion', new_model_session_path %></li> 
            
            <% else %>
              <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_model.email %> <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Mes locations', root_path %></li>
                <li><%= link_to 'Modifier mon profil', edit_model_registration_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Déconnexion', destroy_model_session_path, method: :delete %></li> 
              </ul>
            </li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>